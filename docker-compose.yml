services:
  workspace-service:
    image: workspace-service:latest
    container_name: workspace-service
    restart: unless-stopped
    ports:
      - "5605:5605"
    env_file:
      - .env
    environment:
      - TZ=America/Los_Angeles
      - VAULT_SERVICE_URL=${VAULT_SERVICE_URL}
      - VAULT_SERVICE_TOKEN=${VAULT_SERVICE_TOKEN}
    volumes:
      - workspace-data:/workspace
      - /usr/share/zoneinfo/America/Los_Angeles:/etc/localtime:ro
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "-O", "/dev/null", "http://localhost:5605/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          memory: 256M

volumes:
  workspace-data:
